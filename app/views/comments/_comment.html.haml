.comment
  = gravatar_for(comment, :size => 60).html_safe
  %h3.author
    = comment.email == CONFIG["pitslamp"]["gravatar_email"] ? "me" : ""
    = link_to_unless !comment.url, comment.author, comment.url, :class => "comment_author"
    %span.reaction_time= "reageerde #{distance_of_time_in_words(@post.created_at, comment.created_at)} later"

  %p.comment_content= comment.content

  - if admin?
    %p.admin= link_to "<img src=\"/images/delete.gif\" alt=\"Remove\" />", comment_path(comment), :confirm => 'Are you sure you want to destroy this comment?', :method => :delete, :title => "Remove this comment"